name: growth
description: "Metrics-driven configuration for 1-N growth phase. PRD, UXD, and FAQ required."
extends: startup

spec_config:
  # Source specs: PRD and UXD required
  prd:
    required: true
    category: source
  uxd:
    required: true
    category: source

  # GTM specs: FAQ required for support at scale
  faq:
    required: true
    category: gtm
